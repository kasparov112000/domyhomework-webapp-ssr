apiVersion: v1
kind: Service
metadata:
  name: {{ include "webapp-ssr.fullname" . }}
  labels:
    {{- include "webapp-ssr.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "webapp-ssr.selectorLabels" . | nindent 4 }}