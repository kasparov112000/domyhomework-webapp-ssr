apiVersion: v1
kind: Service
metadata:
  name: {{ include "webapp-dmh-ssr.fullname" . }}
  labels:
    {{- include "webapp-dmh-ssr.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "webapp-dmh-ssr.selectorLabels" . | nindent 4 }}